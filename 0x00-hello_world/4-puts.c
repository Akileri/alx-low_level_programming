#!/bin/bash
#include<studio.h>

/**
 * main - Entry point
 *
 * Description: Writes to stdout with puts function.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
   puts("Programming is like buildin a multilingual puzzle");
 return (0);
 }
